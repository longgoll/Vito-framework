// Vito Web Framework Engine v1.2.0 (vito/src/vito.vit)
// Core Module Exports (Barrel Export — Modular Architecture)
import { listenTcp, TcpListener, TcpStream } from "std/net";
import { charAt } from "std/string";
extern function __vit_str_regex_match(str: string, pattern: string): number;
extern function vit_net_socket_is_connected(fd: number): number;

// Re-export core submodules
import { getPathSegment, getPathSegmentCount, getSubpathFromSegment, routeHasWildcard, routeHasOptional, getRouteType, parseQueryParam, parseHeaderParam } from "src/core/utils.vit";
import { Request, parseHttpMethod, parseHttpPath, parseQueryString, parseHttpBody, parseHttpRequest } from "src/core/request.vit";
import { Response } from "src/core/response.vit";
import { matchRoutePattern, pathMatchesPattern } from "src/core/matcher.vit";
import { Container, createContainer } from "src/core/container.vit";
import { VitoGroup, VitoApp, createApp, createStrictApp, runVitoApp } from "src/core/app.vit";
